msg = "Good Morning"  # Define the original message
new_msg = 'g' + msg[1:]  # Replace the first character 'G' with 'g' and concatenate it with the rest of the string
print(new_msg)  # Print the new message
