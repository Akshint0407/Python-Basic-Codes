# Define a lambda function that multiplies two numbers
x = lambda x, y: x * y  # lambda function takes two arguments x and y and returns their product

# Call the lambda function with 10 and 5 as arguments and print the result
print(x(10, 5))  # Output: 50
